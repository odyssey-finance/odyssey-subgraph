type PositionRegistry @entity {
  # position registry address
  id: Bytes!
  # timestamp when data was updated
  updatedAt: BigInt!
  # current owner of PositionRegistry
  owner: Bytes!
  # fee collector address
  feeCollector: Bytes!
  # number of positions created
  positionCount: BigInt!
  # number of smart accounts created
  smartAccountCount: BigInt!
  # smart accounts
  smartAccounts: [SmartAccount!]! @derivedFrom(field: "positionRegistry")
  # strategies
  strategies: [Strategy!]! @derivedFrom(field: "positionRegistry")
  # daily snapshots
  snapshots: [PositionRegistrySnapshot!]! @derivedFrom(field: "positionRegistry")
}

type Position @entity {
  # position address
  id: Bytes!
  # owner of position (i.e. smart account)
  owner: SmartAccount!
  # strategy id
  strategyId: BigInt!
  # timestamp when position was created
  createdAt: BigInt!
  # timestamp when position was updated
  updatedAt: BigInt!
  # timestamp when position was opened and assets are deposited
  openedAt: BigInt!
  # timestamp when position was closed
  closedAt: BigInt!
  # total transactions
  txCount: BigInt!
  # asset aka collateral address
  asset: Bytes!
  # total assets allocation in this position
  totalAllocated: BigInt!
  # total assets deposited in this position.
  totalDeposited: BigInt!
  # Optional: borrow token address.
  borrowToken: Bytes!
  # Optional: total assets borrowed by this position.
  totalBorrowed: BigInt!
  # price per share
  pricePerShare: BigInt!
  # whether position implementation is outdated
  isOutdated: Boolean!
  # daily snapshots
  snapshots: [PositionSnapshot!]! @derivedFrom(field: "position")
}

type Strategy @entity {
  # strategy id, stored on-chain as strategyId.
  id: String!
  # position registry address
  positionRegistry: PositionRegistry!
  # latest implementation address
  implementation: Bytes!
  # current feePolicy for this strategy
  feePolicy: Bytes!
  # flag indicating whether strategy is active
  isActive: Boolean!
}

type SmartAccount @entity {
  # smart account address
  id: Bytes!
  # position registry address
  positionRegistry: PositionRegistry!
  # timestamp when data was updated
  updatedAt: BigInt!
  # total position of this smart account
  positionCount: BigInt!
  # positions of this smart account
  positions: [Position!]! @derivedFrom(field: "owner")
  # daily snapshots
  snapshots: [SmartAccountSnapshot!]! @derivedFrom(field: "smartAccount")
}

type PositionRegistrySnapshot @entity {
  id: String!
  dayStartTimestamp: BigInt!
  createdAt: BigInt!
  positionCount: BigInt!
  smartAccountCount: BigInt!
  positionRegistry: PositionRegistry!
  smartAccountSnapshots: [SmartAccountSnapshot!]! @derivedFrom(field: "positionRegistrySnapshot")
}

type SmartAccountSnapshot @entity {
  id: String!
  dayStartTimestamp: BigInt!
  createdAt: BigInt!
  smartAccount: SmartAccount!
  positionRegistrySnapshot: PositionRegistrySnapshot!
  positionSnapshots: [PositionSnapshot!]! @derivedFrom(field: "smartAccountSnapshot")
}

type PositionSnapshot @entity {
  id: String!
  dayStartTimestamp: BigInt!
  createdAt: BigInt!
  totalDeposited: BigInt!
  totalBorrowed: BigInt!
  pricePerShare: BigInt!
  position: Position!
  smartAccountSnapshot: SmartAccountSnapshot!
}
